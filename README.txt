EVALUATE EQUATION:

tokenize:
    - split the equation on keywords
    ex 5+4*55-3sin(10) has tokens 5, +, 4, *, 55, -, 3, sin(, 10, )

parse:
    - go through tokens and evaluate according to the grammar